{% extends 'base.html.twig' %}

{% block title %}{{ isNew ? "Ajouter" : "Modifier" }} une adresse{% endblock %}

{% block body %}

<h1 class="text-center">{{ isNew ? "Ajouter" : "Modifier" }} une adresse</h1>

{{form_start(form, {
    'attr': {
        'class': 'container-fluid my-2 formPassUpdate',
        'novalidate': 'novalidate',
    }
})}}
    
    <div class="row">
        <div class="col-6 formRes">
            {{form_row(form.firstname, {'attr': { 'class': 'form-control' }})}}
        </div>
        <div class="col-6 formRes">
            {{form_row(form.lastname, {'attr': { 'class': 'form-control' }})}}
        </div>
    </div>
    {{form_row(form.name, {'attr': { 'class': 'form-control' }})}}
    <div class="row">
        <div class="col-4 formRes">
            {{form_row(form.zipcode, {'attr': { 'class': 'form-control' }})}}   
        </div>
        <div class="col-4 formRes">
            {{form_row(form.city, {'attr': { 'class': 'form-control' }})}}  
        </div>
        <div class="col-4 formRes">
            {{form_row(form.country, {'attr': { 'class': 'form-control' }})}}   
        </div>
    </div>
    
    <div class="d-flex justify-content-center col-12 mt-4">
        <button type="submit" class="btn btn-primary btn-lg c">{{isNew ? "Créer" : "Modifier"}}</button>
    </div>

{{form_end(form)}}
{% endblock %}
