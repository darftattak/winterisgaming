{% extends 'base.html.twig' %}

{% block title %}{{ isNew ? "Ajouter" : "Modifier" }} une adresse{% endblock %}

{% block body %}

{{form_start(form)}}

    {{form_row(form.name)}}
    {{form_row(form.zipcode)}}
    {{form_row(form.city)}}
    {{form_row(form.country)}}

    <button type="submit">{{isNew ? "Créer" : "Modifier"}}</button>

{{form_end(form)}}
{% endblock %}
